# 中试 & 量产订单监控仪表盘 — 需求共识（grill-me 产出）

> 本文档由 grill-me 会话厘清，作为设计与实现的统一依据。

## 1. 用户与目的
- **核心用户**：业务员
- **核心场景**：打开仪表盘 → 看订单进度 → 跟客户同步"这单到哪了"
- **设计导向**：准确、可读、能对客户解释；不是车间实时干预，不是高层汇报

## 2. 整体架构：双模块
- **模块 A：试制进度监控**（myexcel，工序流）
- **模块 B：量产交付监控**（SAP 055，交付流）
- 两模块订单号体系不同（送样单号 vs 销售订单号），**不做强行关联**
- **默认进模块 B（量产）**，支持用户自定义模块顺序（本地记忆，不强制全用户统一）
- 两模块都按"**当前业务员自己的订单**"过滤首屏

## 3. 归属机制（首屏过滤依据）
- 用 **RPA 定期生成"订单-业务员"映射表**，仪表盘按此表过滤
- 统一试制与量产两套逻辑，绕开 myexcel 侧无归属字段的限制
- 不依赖 SAP 的"销售助理/订单创建人"或 myexcel 的"开单人员"做归属（这些字段语义不等于"跟进业务员"）

## 4. 数据源

### 4.1 试制侧（myexcel，4 套异构表）
| 类型 | 主表 | order_no 字段 | 示例 |
|---|---|---|---|
| 小试 | tabdiytable5992 | f102935 | EX20260604-796 |
| 中试广州 | tabdiytable3393 | f52977 | 12482359 |
| 中试上海 | tabdiytable5724 | f96425 | — |
| OA辅助单 | tabdiytable8460 | f138799 | — |

- 人员映射表：tabdiytable3439（基地-工序-人员）
- 维度：基地(广州/上海)、机台、数量、试制类型

### 4.2 量产侧（SAP 055）
字段清单：
> 过账日期、物料、工厂、发货数量、发货数量-吨、售达方名称、批次、售达方、送达方、送达方名称、客户物料号、物料描述、销售助理、订单创建人、销售订单、交货单、送货单号、客户订单号、创建日期、地区、城市、送货地址、销售组织描述、销售办公室描述、销售组描述、订单类型、描述、物料类型、库位、销售组织、工厂描述、库位描述、销售组、产品线、是否按期交付、首个日期、在途天数

## 5. 进度形态

### 5.1 试制侧（工序流）
```
开单 → 色粉计量 → 混料 → 挤出 → 注塑色板 → 注塑样条 → 最终班长完成 → 排产
```
- 每节点带【时间 + 责任人 + 是否异常】
- 3 类异常字段需高亮：挤出异常、注塑样条异常、注塑色板异常

### 5.2 量产侧（交付流）
```
创建日期 → 首个日期(预计交期) → 过账日期(实际发货) → 在途 → 签收(是否按期交付)
```
- 核心指标：**是否按期交付**、**在途天数**、预计交期 vs 实际发货

## 6. 首屏形态
- **列表 + 详情两层结构**
- 列表每行直接展示**进度概览**，点行进详情看完整时间轴
- 试制行：当前工序 + 已完成/总节点数 + 异常标记（如"注塑样条中 5/8"，异常整行标红）
- 量产行：当前阶段 + 是否按期 + 在途天数（如"在途 · 按期 · 3天"或"已发货 · 逾期"）

## 7. 试制模块的 4 类订单组织
- **合到一个列表**，类型作为列 + 顶部筛选器，**不分 tab**
- 4 类表字段结构高度相似（都是工序流），合并展示无障碍

## 8. 设计要点
1. 双模块风格统一但形态不同：试制=工序步骤条；量产=交付时间轴
2. 检索：按订单号/客户/产品/物料查找
3. 量产的"是否按期交付/在途天数"要醒目，是业务员答客户的核心
4. 试制的异常节点要高亮
5. 受众是业务员，不是分析师：图表别堆，"找订单 + 看进度"优先

## 9. 数据获取
- **定时拉取存本地库**（不直连源系统）
- 拉取频率：待定（建议每 15 分钟 ~ 每小时）
- 仪表盘查本地库，快、不卡源系统、可回溯历史

## 10. 待定项（不阻塞设计，先按推荐出）
- 责任人是否对外展示（推荐：试制模块内部可见，对客户隐藏）
- 异常高亮的具体阈值规则
- 拉取频率
- 量产侧"签收"节点是否有实际数据源（SAP 字段里未见明确签收字段，可能"是否按期交付"即终态）

---

## grill-me 决策树回顾
| # | 分支 | 决策 |
|---|---|---|
| 1 | 核心用户 | 业务员，跟客户同步进度 |
| 2 | 数据源 | SAP 055 + myexcel 4 套表 |
| 3 | 数据获取 | 定时拉取存本地库 |
| 4 | 两套系统关联 | 不关联，双模块 |
| 5 | 默认模块 | 量产（B），可自定义顺序 |
| 6 | 首屏形态 | 列表+详情，列表带进度概览 |
| 7 | 首屏过滤 | 按业务员自己的订单（RPA 映射表） |
| 8 | 试制 4 类组织 | 合并列表 + 类型筛选，不分 tab |
